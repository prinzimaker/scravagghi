{
  "name": "scravagghi-client",
  "version": "0.1.0",
  "description": "Scravagghi Game Client - Artillery game with beetles",
  "type": "module",
  "scripts": {
    "generate-sounds": "node ../scripts/generate-sounds-manifest.js",
    "predev": "npm run generate-sounds",
    "dev": "vite",
    "prebuild": "npm run generate-sounds",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  },
  "devDependencies": {
    "vite": "^7.3.0"
  }
}
